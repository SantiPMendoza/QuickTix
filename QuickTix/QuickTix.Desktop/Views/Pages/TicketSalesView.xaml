<Page x:Class="QuickTix.Desktop.Views.Pages.TicketSalesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QuickTix.Desktop.Views.Pages"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SalesView">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>



        <!-- ListView con reservas -->
        <ListView Grid.Row="1"
                  Margin="50"
                  HorizontalContentAlignment="Stretch"
                  MaxHeight="600"
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem}">

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="Fecha de venta" Width="180" DisplayMemberBinding="{Binding Date, StringFormat=\{0:dd/MM/yyyy HH:mm\}}" />
                    <GridViewColumn Header="Día Semana" Width="120" DisplayMemberBinding="{Binding DiaSemanaString}" />
                    <GridViewColumn Header="Vendido por" Width="100" DisplayMemberBinding="{Binding ManagerName}" />
                    <GridViewColumn Header="Número de entradas" Width="150" DisplayMemberBinding="{Binding Quantity}" />
                    <GridViewColumn Header="Precio" Width="120" DisplayMemberBinding="{Binding TotalAmount}" />

                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
