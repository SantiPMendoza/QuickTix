<Page x:Class="QuickTix.Desktop.Views.Pages.SalesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="650"
      d:DesignWidth="1000"
      Title="SalesView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="1" Margin="30">

            <TabItem Header="Entradas">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Recargar" Width="120"
                                Command="{Binding TicketSales.LoadAsyncCommand}" />
                    </StackPanel>

                    <ListView Grid.Row="1"
                              HorizontalContentAlignment="Stretch"
                              MaxHeight="600"
                              ItemsSource="{Binding TicketSales.Items}"
                              SelectedItem="{Binding TicketSales.SelectedItem}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="ID" Width="60" DisplayMemberBinding="{Binding Id}"/>
                                <GridViewColumn Header="Fecha de venta" Width="180"
                                                DisplayMemberBinding="{Binding Date, StringFormat=\{0:dd/MM/yyyy HH:mm\}}"/>
                                <GridViewColumn Header="Día Semana" Width="120" DisplayMemberBinding="{Binding DiaSemanaString}"/>
                                <GridViewColumn Header="Recinto" Width="160" DisplayMemberBinding="{Binding VenueName}"/>
                                <GridViewColumn Header="Vendido por" Width="140" DisplayMemberBinding="{Binding ManagerName}"/>
                                <GridViewColumn Header="Nº entradas" Width="110" DisplayMemberBinding="{Binding Quantity}"/>
                                <GridViewColumn Header="Precio unit." Width="110" DisplayMemberBinding="{Binding UnitPrice}"/>
                                <GridViewColumn Header="Total" Width="110" DisplayMemberBinding="{Binding TotalAmount}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </TabItem>

            <TabItem Header="Suscripciones">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Recargar" Width="120"
                                Command="{Binding SubscriptionSales.LoadAsyncCommand}" />
                    </StackPanel>

                    <ListView Grid.Row="1"
                              HorizontalContentAlignment="Stretch"
                              MaxHeight="600"
                              ItemsSource="{Binding SubscriptionSales.Items}"
                              SelectedItem="{Binding SubscriptionSales.SelectedItem}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="ID" Width="60" DisplayMemberBinding="{Binding Id}"/>
                                <GridViewColumn Header="Fecha de venta" Width="180"
                                                DisplayMemberBinding="{Binding Date, StringFormat=\{0:dd/MM/yyyy HH:mm\}}"/>
                                <GridViewColumn Header="Día Semana" Width="120" DisplayMemberBinding="{Binding DiaSemanaString}"/>
                                <GridViewColumn Header="Recinto" Width="160" DisplayMemberBinding="{Binding VenueName}"/>
                                <GridViewColumn Header="Vendido por" Width="140" DisplayMemberBinding="{Binding ManagerName}"/>
                                <GridViewColumn Header="Cliente" Width="180" DisplayMemberBinding="{Binding ClientName}"/>
                                <GridViewColumn Header="Categoría" Width="160" DisplayMemberBinding="{Binding SubscriptionCategory}"/>
                                <GridViewColumn Header="Precio" Width="110" DisplayMemberBinding="{Binding Price}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</Page>
